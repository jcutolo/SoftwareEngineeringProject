getProviderDirectory():
	Retrieve provider directory list as object
updateProviderDirectory():
	Ask user for service name
	if service name value is invalid:
		Print error code
	else:
		Ask user for service fee
		if service fee value is invalid:
			Print error code
		else:
			Add new service object to provider directory list

	
	