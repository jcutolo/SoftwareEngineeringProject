Global boolean LoggedIn = False
signIn():
	Wait for user input
	if provider ID is valid:
		LoggedIn equal True
	else:
		Print error to console
validateMember():
	Wait for provider to input member ID
	if member ID is valid:
		Load Member object from database
		Allow for member based functionality
	else:
		Print error to console
lookUpServiceCode():
	Call getProviderDirectory function from Provider Controller Class
	Print service code with respected service name
lookUpFeeToBePaid():
	Call getProviderDirectory function from Provider Controller Class
	Print Fees with respected service code
